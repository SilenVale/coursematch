# CourseMatch 协作规则

本文件面向参与 CourseMatch 的开发者及其 AI 编程工具。

## 开始任务前

1. 阅读 `README.md`、`docs/PROJECT_CONTRACT.md` 和 `docs/COLLABORATION.md`。
2. 在项目根目录执行 `git status`，确认当前分支和未提交改动。
3. 先确认对应的 GitHub Issue、负责范围与验收标准；没有明确任务时，不开始实现。
4. 从最新 `master` 创建自己的任务分支，不直接修改或推送 `master`。

## 修改边界

- 只修改当前任务需要的文件；发现共享文件、需求或数据结构需要改变时，先在群里说明并等待负责人确认。
- 不擅自引入大型框架、付费服务或第三方账号依赖。
- 不提交 `.env`、密钥、个人配置、`node_modules`、构建产物、临时日志或录屏文件。
- 当前技术栈尚未确定，不得虚构安装、构建或测试命令。

## 提交与合并

1. 修改后检查 `git diff` 和 `git status`，确认没有范围外改动。
2. 运行当前项目实际存在的检查；若尚无检查命令，在 Pull Request 中明确写“未配置自动检查”。
3. 只暂存当前任务相关文件，提交信息使用 `type: 简要说明`，例如 `docs: 补充项目立项说明` 或 `feat: 完成题目列表接口`。
4. 推送个人分支，创建 Pull Request，写明目标、改动、验证结果和风险。
5. 不自行合并 Pull Request；由仓库负责人或指定审查人合并。

## AI 使用约定

给 AI 分配任务时，必须带上：任务目标、允许修改范围、不能碰的文件、验收条件和当前分支名。

AI 声称“完成”不是验收。必须根据实际 diff、可运行结果和测试证据判断。
